import React from "react";

const JourneysList: React.FC<{ journeys: any[] }> = (props) => {
  return (
    <React.Fragment>
      <div>Journelist component</div>
    </React.Fragment>
  );
};

export default JourneysList;
